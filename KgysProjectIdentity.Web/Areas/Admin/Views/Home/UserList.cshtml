@using KgysProjectIdentity.Web.Taghelper
@{
    ViewData["Title"] = "UserList";
}
@model             List<KgysProjectIdentity.Core.Areas.Admin.ViewModel.UserViewModel>
<h1>Kullanıcı Bilgileri</h1>


<table class="table table-hover">
    <thead>
        <tr>
            <th>Id</th>
            <th>User Name</th>
            <th>Email></th>
            <th>Roller</th>
            <th>Rol İşlemleri</th>
        </tr>
    </thead>
    <tbody>
        @foreach(var item in Model)
        {
            <tr>
                <td>@item.Id</td>
                <td>@item.Name</td>
                <td>@item.Email</td>
                <td>
                   ??????
                <td>
                    <a asp-controller="Roles" asp-action="AssignRoleToUser" asp-route-id="@item.Id">Rol Ata</a>
                </td>
            </tr>
        }
    </tbody>
</table>
